#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // Start writing your code here
    int n; int x;
    cin>>n>>x;
    vector<int>w(n);
    for(int i{0}; i<n; ++i) cin>>w[i];
    
    return 0;
}